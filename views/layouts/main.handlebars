<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Link Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel='icon' type='image/png' href='assets/images/lilypads/lilypad G1.png' />
    <link
        href="https://fonts.googleapis.com/css2?family=Indie+Flower&family=Lilita+One&family=Patrick+Hand&family=Shadows+Into+Light&family=Titan+One&display=swap"
        rel="stylesheet">
    <!-- Link Bootstrap -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
    <!-- Link Stylesheet -->
    {{#if logged_in}}
    <link rel="stylesheet" href="./assets/css/index.css">
    {{else}}
    <link rel="stylesheet" href="./assets/css/index.css"> 
    <link rel="stylesheet" href="./assets/css/homepage.css">
    {{/if}}
    <title>Welcome to our Pad!</title>
</head>

<body>
    <header class="container">
        <div class="row">
            <h1 id="main-title">Leap To Success</h1>
            {{#if logged_in}}
            <button id="logout" class="btn-no-style">Logout</button>
            {{else}}
            {{!-- Don't need anything here --}}
            {{/if}}
        </div>
    </header>

    <main class="container container-fluid mt-5">
        <!-- Render the sub layout -->
        {{{ body }}}
    </main>

    <footer>

    </footer>

    <!-- JS Dependencies -->
       <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.0/jquery.min.js"></script>
    <script src=" https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="./scripts/login.js"></script>
    <script src="./scripts/signup.js"></script>
    <script src="./scripts/logout.js"></script>
</body>

</html>